{
  "name": "almuerzos",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare-android": "npx cap add android",
    "open-android": "npx cap open android",
    "copy-web": "npx cap copy android",
    "build-web": "echo \"No build step: app is static. Ensure files are in project root (index.html, app.js, styles.css)\""
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
